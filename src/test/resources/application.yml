spring:
# 해당 설정들이 없으면 spring boot 에서 자동으로 memory 모드를 default 로 사용한다.
#  datasource:
#    url: jdbc:h2:mem:test # memory mode 로 동작 (테스트를 위함)
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop # drop - create
#    properties:
#      hibernate:
##        show_sql: true  // System.out 에 하이버네이트 실행 SQL을 남김
#        format_sql: true

logging.level:
  org.hibernate.SQL: debug # logger를 통해 하이버네이트 실행 SQL 을 남김
  org.hibernate.type: trace